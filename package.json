{
  "name": "agentdock",
  "version": "0.2.0",
  "private": true,
  "author": {
    "name": "jiegokoji",
    "email": "jiegokoji@gmail.com"
  },
  "homepage": "https://github.com/jiegokoji",
  "description": "A cross-platform desktop workspace for CLI coding agents",
  "desktopName": "AgentDock",
  "main": "electron/main.cjs",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"wait-on http://127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build": "tsc --noEmit && vite build",
    "test": "node --test test/*.test.cjs",
    "start": "electron .",
    "dist": "npm run build && node scripts/package.cjs",
    "dist:win": "npm run build && node scripts/package.cjs --win --x64",
    "dist:linux": "npm run build && node scripts/package.cjs --linux --x64",
    "dist:mac": "npm run build && node scripts/package.cjs --mac --arm64",
    "release": "node scripts/build-release.cjs",
    "release:plan": "node scripts/build-release.cjs --plan",
    "release:fast": "node scripts/build-release.cjs --skip-tests"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@vitejs/plugin-react": "^6.0.4",
    "lucide-react": "^1.25.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "vite": "^8.1.5"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^43.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.2",
    "wait-on": "^8.0.3"
  },
  "build": {
    "appId": "dev.agentdock.desktop",
    "productName": "AgentDock",
    "files": ["dist/**/*", "electron/**/*", "build/icon.png", "package.json"],
    "directories": { "output": "release", "buildResources": "build" },
    "win": { "target": "nsis", "icon": "build/icon.ico" },
    "mac": { "target": "dmg", "icon": "build/icon.icns" },
    "linux": { "target": ["AppImage", "deb"], "icon": "build/icon.png", "category": "Development" }
  }
}
